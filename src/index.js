import Equalize from './lib/equalize/Equalize';
import EqualizerContainer from './lib/equalize/EqualizerContainer';

export { Equalize, EqualizerContainer };

export default {
	Equalize,
	EqualizerContainer,
};
